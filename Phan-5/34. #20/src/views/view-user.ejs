<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View-user</title>

    <!-- Định nghĩa bootstrap -->
    <!-- prettier-ignore -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>
  <body>
    <div style="width: 600px; margin: auto" >
        <h3 style="padding: 20px 0px;">View detail a user id = <%= id %></h3>
        <!-- route  /handle-update-user-->
        <form action="/handle-update-user" method="post">
            <!-- mb: margin-bottom -->
            <div class="mb-3">     
                <label class="form-label">Name: </label>
                <!-- Sử dụng name để gửi dữ liệu cho Backend (Server) -->
                <!-- Cần có thuộc tính value để hiển thị -->
                <input value="<%= user.name%>" name="fullName" type="text" class="form-control" placeholder="Your name">  
            </div>
            <div class="mb-3">     
                <label class="form-label">Email: </label>
                <input value="<%= user.email%>" name="email" type="email" class="form-control" placeholder="name@example.com">
            </div>
            <div class="mb-3">
                <label class="form-label">Address</label>
                <!-- textarrea không có thuộc tính value -->
                <textarea name="address" class="form-control" rows="2"><%= user.address%></textarea>
            </div>
            <div class="mb-3">
                <!-- Đổi thành Update -->
                <button type="submit" class="btn btn-warning">Update</button>
            </div>
        </form>
    </div>

    <!-- Định nghĩa bootstrap -->
    <!-- prettier-ignore -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
